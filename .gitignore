node_modules
dist
.turbo
*.tsbuildinfo
.DS_Store
.env
.env.local

# Autogenerated by `varlock typegen` — regenerates on schema change or vite plugin runs.
# Produced via the `prepare` script (runs on `bun install`) and in CI before typecheck/build.
env.d.ts
